/**
 * Brand colour constants — single source of truth for the teal→blue gradient
 * used across the app. Import these instead of repeating the string literals.
 */

/** Full-area background gradient (banners, tiles, buttons, cards) */
export const BRAND_GRADIENT = 'linear-gradient(135deg, #00C9A7, #0077B6)'

/** Horizontal progress bar fill gradient */
export const BRAND_GRADIENT_H = 'linear-gradient(90deg, #00C9A7, #0077B6)'
